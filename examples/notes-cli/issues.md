| id | state | owner | summary | first seen | last updated (t) | history |
|---|---|---|---|---|---|---|
| I1 | closed | developer | R5: `python -m notes` entry point plus the exit-2 usage line, with the line naming every subcommand. The `search` half is now in place — the single line is `usage: python -m notes {add <text> \| list [--color] \| done <n> \| search <query>}` and every usage-error form prints it. | prd | 3 | open@1, targeted@1 as T1-1, entry_point_verified@evidence-1 V1-1/V1-2, targeted@2 as T2-1, verified@evidence-2 V2-3/S-R5 (9 forms + 7 flag-misuse forms, all rc 2), closed@3, re-verified@evidence-3 V3-6/S-R5 (16 forms, still rc 2) |
| I2 | closed | developer | R6: JSON store with shape validation. Invalid JSON or a mis-shaped store makes every subcommand name the store path on stderr and exit 4, with no traceback and no silent overwrite. | prd | 3 | open@1, targeted@1 as T1-2, verified@evidence-1 V1-5/V1-6 (27 corrupt-store runs), closed@2, re-verified@evidence-3 V3-4 (90 corrupt-store runs, 0 deviations, undisturbed by the new `save` guard) |
| I3 | closed | developer | R1: `add <text>` appends a note, exits 0, and a following `list` shows the text. | prd | 2 | open@1, targeted@1 as T1-2, verified@evidence-1 V1-3, closed@2, re-verified@evidence-3 V3-7/S-R1 |
| I4 | closed | developer | R2: `list` prints `<n>. [ ] <text>` in insertion order with `n` from 1, and `[x]` for done notes (correct past 9). | prd | 2 | open@1, targeted@1 as T1-2, verified@evidence-1 V1-3/V1-7, closed@2, re-verified@evidence-3 V3-7/S-R2 (byte-identical to evidence-2) |
| I5 | closed | developer | R3: `list` against an empty or absent store prints exactly `no notes`, exits 0, and creates neither the store file nor a parent directory. | prd | 2 | open@1, targeted@1 as T1-2, verified@evidence-1 V1-4, closed@2, re-verified@evidence-3 V3-8/S-R3 (9 bytes, nothing created, with and without `--color`) |
| I6 | closed | developer | R4: `done <n>` marks a note done with exit 0 and is idempotent; a non-positive-integer or out-of-range `n` exits 3 naming the argument on stderr with the store unchanged. | prd | 2 | open@1, targeted@1 as T1-3, verified@evidence-1 V1-7/V1-8, closed@2, residual input-canonicalisation defect split out as I11, re-verified@evidence-3 V3-10/S-R4 |
| I7 | closed | developer | R7: `search <query>` matches a case-insensitive substring and prints matches in the `list` format at their full-list positions (so a number from `search` is safe to feed to `done`); no match prints exactly `no matches` and exits 0, without creating the store. | prd | 3 | open@1, deferred@1, targeted@2 as T2-1, verified@evidence-2 V2-1/V2-2/S-R7, closed@3, re-verified@evidence-3 V3-11 (the `search ""` consequence is still unpinned by a test — tracked as I18) |
| I8 | closed | developer | R8 byte half: `list --color` wraps only a done note's text as `\x1b[2m<text>\x1b[0m`, leaves the prefix and not-done lines byte-identical, accepts the flag on `list` only, and gates the escapes on the flag alone and never on `isatty` (verified on a real pty and on a pipe). The appearance half stays with I9. | prd | 3 | open@1, deferred@1, targeted@2 as T2-2, verified@evidence-2 V2-4/V2-5/V2-6/S-R8, closed@3, re-verified@evidence-3 V3-12 (pty and pipe, `the flag alone decides: True`) |
| I9 | open | user | R8 appearance: whether the dim rendering is legible in a real terminal on a dark background is a human judgement, not scriptable. The escape bytes are verified; a human runs `export NOTES_FILE=$(mktemp -u); .venv/bin/python -m notes add "still to do" && .venv/bin/python -m notes add "already done" && .venv/bin/python -m notes done 2 && .venv/bin/python -m notes list --color` on their usual background and confirms the dimmed line is readable. If dim is illegible the fix is a different SGR code at `src/notes/cli.py:28`. Never a Target. | prd | 3 | open@1, still_blocked@evidence-1 G1-4, unblocked_by@2 T2-2, actionable_for_the_first_time@evidence-2 G2-4 (bytes verified, appearance still open), validation V3-17@3, still_open@evidence-3 G3-1 (V3-17 unverifiable by script; the single reason `qa_status: fail` and `coverage: incomplete` at the final iteration, as prd.md:70-73 prescribes) |
| I10 | closed | developer | Non-functional (prd section 4): every requirement R1–R8 has at least one test in `tests/`, and no test touches the real home directory. All eight are named in test docstrings and the suite is green. The separate coverage hole for section 3's store-path rule was tracked as I15. | prd | 3 | open@1, partially targeted@1 via T1-1..T1-3, partially verified@evidence-1 V1-12 (R1-R6), targeted@2 via T2-1/T2-2, verified@evidence-2 V2-8 (R7/R8 docstrings, 31 passed, home-leak grep empty), closed@3, re-verified@evidence-3 V3-2/V3-16 (37 passed, R1…R8 docstrings intact through the T3-1 split, real `$HOME/.notes.json` absent before and after the suite) |
| I11 | closed | developer | R4: `cli._parse_index` accepted non-canonical integer spellings through bare `int()`, so `done 1_0` silently marked note 10. Now only a canonical ASCII decimal without a leading zero is accepted; 24 rejected spellings all exit 3 naming the argument with the store byte-unchanged. | evidence-1 | 3 | open@2 (G1-5), targeted@2 as T2-3, verified@evidence-2 V2-7/S-R4 (24 spellings, incl. fullwidth `１` and `\t1`), closed@3, re-verified@evidence-3 V3-10 (24 spellings, 0 deviations, also under CPython 3.9.25) |
| I12 | closed | developer | `store.save` guarded nothing, so an unwritable store path (read-only directory, full disk) produced an 8-frame traceback and exit 1 instead of a one-line message. Fixed as decided: `save` wraps both `parent.mkdir` and `write_text` so `OSError` becomes `StoreError`, which `main` maps to exit 4; no fifth exit code. The message `notes: unwritable store <path>: cannot be written (Permission denied)` stays distinguishable from the invalid-JSON reason. | evidence-1 | 3 | open@2 (G1-6), deferred@2 with the exit-code decision recorded, re-observed unchanged@evidence-2 G2-2, targeted@3 as T3-2, verified@evidence-3 V3-3 (`add`/`done` rc 4, 1 stderr line naming the path, 0 stdout, no traceback, both parent-exists and parent-must-be-created shapes, store untouched) with V3-4 showing the 90 corrupt-store runs undisturbed, closed@3 |
| I13 | open | developer | `add ""` and `add "   "` store a blank note that lists as a bare `1. [ ] ` and cannot be removed (there is no delete subcommand). The PRD does not define this. Planner decision, unchanged: treat empty or whitespace-only text as a usage error (exit 2 with the existing usage line); no new exit code, one test. Cheapest to settle together with I16, since both concern input the PRD never described. | evidence-1 | 3 | open@2 (G1-7), deferred@2 with the behaviour decision recorded, re-observed unchanged@evidence-2 G2-3, deferred@3 (ranked below T3-3's unmet section-3 coverage; changing an exit-0 input path on the last iteration was judged not worth the regression risk), re-observed unchanged@evidence-3 G3-3 (minor, owner developer), still_open@3 — the loop ended at t=T=3 with this unfixed |
| I14 | closed | developer | `tests/test_cli.py` was 303 lines against `hoh/project.md`'s "a module over 200 lines gets split". Split into one file per area with no assertion weakened, renamed away or dropped. | evidence-2 | 3 | regressed@3 (G2-1; the module-size clause was verified@evidence-1 V1-11 and re-graded as a gap in evidence-2 P2-9), targeted@3 as T3-1, verified@evidence-3 V3-1/V3-15 (longest file `src/notes/cli.py` 146, `tests/test_cli.py` 303 → 40, none over 200; every function under 50 lines, longest 25) plus evidence-3's faithfulness audit (all 16 test names present one each, assertions 92 → 116, tests 31 → 37, additions only), closed@3 |
| I15 | closed | developer | prd.md section 3's store-path rule (`NOTES_FILE` when set and non-empty, otherwise `~/.notes.json`) had no test behind it and no V-id or P-id in plan-2. Now covered by `tests/test_store_path.py` and reproduced black-box with a redirected `HOME`. | evidence-2 | 3 | open@3 (G2-5), targeted@3 as T3-3, verified@evidence-3 V3-5 (`tests/test_store_path.py` 4 passed in isolation; redirected-`HOME` repro: `list` → `no notes` rc 0 creating nothing, `add` → rc 0 creating `$HOME/.notes.json`, `list` reads it back; identical for `NOTES_FILE=""`) with V3-16 confirming the real home is never touched, closed@3 |
| I16 | open | user | R8 wording: does "without `--color`, no escape sequence appears in the output at all" cover escape bytes stored inside a note's own text? Plain `list` echoes them verbatim (`ESC count = 2`), and has since iteration 1 (`0cdf922`), so this is not a regression. Requires a PRD wording decision first: (a) scope the guarantee to escapes the tool itself emits — amend prd.md:50-51, zero code change, the reading plan-3 already assumes; or (b) sanitize stored text on output, which changes the exact output strings R2 fixes and needs R2 amended in the same breath. Developer implements only if the user chooses (b), in which case V3-7/V3-12 must be re-verified. Best answered together with I13. | evidence-2 | 3 | open@3 (G2-6, owner user per QA's recommendation), re-observed unchanged@evidence-3 G3-2 (minor, owner user; `qa-3-pty.log:46-51`), still_open@3 — the loop ended at t=T=3 with the decision unmade |
| I17 | closed | orchestrator | plan-2 was internally inconsistent: P2-9 said "every module stays under 200 lines" while its V2-12 command measured `src/notes/*.py` only, so the constraint could never fail on a test file and QA had to grade against the constraint text. Fixed in plan-3 by making the constraint and its command name the same file set (`src/notes/*.py tests/*.py`) in P3-11, T3-1, V3-1 and V3-15. The general lesson belongs to the Planner role contract, which the Developer cannot edit — hence owner `orchestrator`, never a Target. | evidence-2 | 3 | open@3 (raised in evidence-2 G2-1's note to the Planner), fixed_pending_verify@3 (plan-3 P3-11/V3-15 and T3-1/V3-1 aligned), closed@3 — verified@evidence-3 V3-18: "no constraint is graded against a wider subject than its own command measures. The plan-2 P2-9/V2-12 mismatch is gone". **Closed rather than kept open**: the one residue QA records is cosmetic wording, not a grading mismatch — P3-10 says "every `.py` file *under* `src/notes/` and `tests/`" while V3-14's command is a non-recursive glob, and `find src tests -name '*.py'` returns exactly the 14 files the glob matches, so the subject and the command coincide empirically. QA explicitly rated it "worth one word in a future plan, not a gap" and filed no G-id for it, so it is recorded here as a drafting reminder for future plans (prefer the glob's own wording over "under") rather than as an open issue |
| I18 | open | developer | `--color` outside `list`, and the empty query, are undefined by the PRD and unpinned by any test: `add --color` exits 0 storing the literal text `--color`, `search --color` treats it as a query, and `search ""` returns every note. All three follow from R7's substring rule and R8 scoping the flag to `list` — the reading plan-3 adopted — but no assertion freezes them, so a refactor could change them with the suite green. Regression exposure, not a defect; no user impact today. Fix is three small tests pinning today's output, no code change. | evidence-3 | 3 | open@3 (G3-4, note severity, owner developer; also noted under V3-11 for `search ""`) |
| I19 | open | developer | The suite pins fewer argument spellings than QA verified: of 9 usage forms and 7 flag-misuse forms all exiting 2, `tests/test_cli_usage.py` pins 8 usage forms (no `done 1 2`) and 5 flag forms (no `list --color --color`, no `list --COLOR`). For those three spellings the only protection is the QA log, not an assertion. Regression exposure only, no user impact. Fix is two one-line edits: add `("done", "1", "2")` to the tuple at `tests/test_cli_usage.py:33-40` and `("list", "--color", "--color")`, `("list", "--COLOR")` to the tuple at `:54-60`. Predates the T3-1 split (`b090366:tests/test_cli.py` had the same tuples). | evidence-3 | 3 | open@3 (G3-5, note severity, owner developer) |
