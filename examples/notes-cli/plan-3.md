---
iteration: 3
status: active
targets: 3
---
# Diagnosis
- Root cause: there is no single defect left. Two residuals were deferred on purpose at iteration 2 — `store.save` guards nothing, so an unwritable path escapes as a traceback with exit 1 (evidence-2 G2-2, confidence high; the same observation as evidence-1 G1-6), and the `NOTES_FILE`-unset fallback has no test behind it (evidence-2 G2-5, confidence high) — plus one regression whose cause is mechanical: every new CLI test from iterations 1 and 2 was appended to the single `tests/test_cli.py`, which is now 303 lines against `hoh/project.md`'s 200-line rule (evidence-2 G2-1, confidence high; it was 163 lines at `0cdf922`).
- Strategy: this is the last iteration (`config.md` `T: 3`), so close what is open rather than start anything new. Take the regression first because everything else lands in the files it moves, then the traceback defect, then the missing default-path test. No production behavior changes except turning one traceback into the already-specified exit 4; no new exit code; no PRD-undefined behavior is altered.
- Stop condition: stop and report if (a) the suite drops below 31 passing, or the passed count disagrees with `gate-3.md`, at any point; (b) the `store.save` guard makes any of the 45 corrupt-store runs (V3-4) deviate; (c) exit 4 turns out not to cover the unwritable case without touching exit 0/2/3 — do not invent a fifth exit code, report instead; (d) the same failure appears twice in a row after a fix attempt.

# Evidence summary (for the Developer, 3 lines max)
- All eight PRD functional requirements are verified black-box on the iteration-2 candidate (evidence-2 S-R1…S-R8), with `31 passed` under both the venv interpreter and real CPython 3.9.25; `qa_status: fail` came only from the PRD-mandated manual gap G2-4 and from the size regression G2-1, not from any Target miss.
- Still broken: `NOTES_FILE` pointing into a `chmod 500` directory gives an 8-frame traceback and exit 1 from `store.save` (evidence-2 G2-2, `qa-2-edge.log` §E2), and `tests/test_cli.py` is 303 lines (evidence-2 G2-1).
- Still uncovered: nothing asserts the `~/.notes.json` fallback when `NOTES_FILE` is unset — the behavior is correct today (evidence-2 S-sec3) but untested (evidence-2 G2-5).

# Targets (priority order, at most 3)
| id | type | title | instruction | target paths | linked issue/gap | done_when | validation |
|---|---|---|---|---|---|---|---|
| T3-1 | regression | Split `tests/test_cli.py` by area so every module obeys the 200-line rule | Move the existing tests out of `tests/test_cli.py` into one file per area, as `hoh/project.md` ("one file per area") requires: usage errors and flag spellings, store shape and the corrupt-store matrix, `done` index canonicality, `search`, `list --color`. Suggested names: `tests/test_cli_usage.py`, `tests/test_cli_store.py`, `tests/test_cli_done.py`, `tests/test_cli_search.py`, `tests/test_cli_color.py`; `tests/test_cli.py` may be deleted or kept as a thin remainder. This is **pure test motion**: no assertion may be weakened, renamed away or dropped, and no file under `src/` is touched by this Target. Carry each test's docstring across unchanged so the `R1`–`R8` mentions survive. Share fixtures via a `tests/conftest.py` if that avoids duplication, and keep pointing `NOTES_FILE` at `tmp_path`. Do the split **before** T3-2 and T3-3 so their new tests land in the right file. | `tests/test_cli.py`, new `tests/test_cli_*.py`, optional `tests/conftest.py` | I14 / G2-1 | `wc -l src/notes/*.py tests/*.py` shows no file over 200 lines, and `.venv/bin/python -m pytest -q` reports at least 31 passed with every one of `R1`…`R8` still named in at least one test docstring | V3-1, V3-2 |
| T3-2 | defect | An unwritable store exits 4 with one line instead of a traceback | Wrap the body of `store.save` so that `OSError` (both the `parent.mkdir` call and the `path.write_text` call) becomes `StoreError`, which `main` already maps to exit 4 — the decision recorded in plan-2 and in ledger issue I12. Do not add a fifth exit code and do not change exit 0/2/3 or any existing message. The stderr output must stay exactly one line that names the store path and must not claim the file is invalid JSON when the real reason is that it cannot be written; a distinct reason string such as `cannot be written (Permission denied)` is expected. `stdout` must stay empty and no traceback may reach the user. Add tests in the split store file covering both shapes of the failure: a `chmod 500` directory that already exists (`$dir/x.json`) and one whose parent must be created (`$dir/sub/x.json`), for `add` and for `done` against an otherwise valid store. Use `tmp_path` and restore the mode afterwards so the temp tree can be cleaned. | `src/notes/store.py`, `tests/test_cli_store.py` (or the split file that owns the store area) | I12 / G2-2 | `NOTES_FILE` inside a `chmod 500` directory makes `add hi` and `done 1` exit **4**, print zero bytes on stdout, print exactly one stderr line naming the path, and print no traceback — in both the existing-directory and the must-create-parent shapes | V3-3, V3-4 |
| T3-3 | requirement | Test the `~/.notes.json` fallback that prd.md section 3 requires | Add a test that asserts the store path when `NOTES_FILE` is unset. Redirect the home directory to `tmp_path` (`monkeypatch.setenv("HOME", str(tmp_path))`, and also `USERPROFILE` if you prefer to be explicit), `monkeypatch.delenv("NOTES_FILE", raising=False)`, then assert `store.resolve_path() == tmp_path / ".notes.json"` and that an `add` followed by a `list` round-trips through that file. Cover the empty-string case (`NOTES_FILE=""` falls back too, `src/notes/store.py:26-32`). The test must never read or write the real home — redirect first, assert second. Give it a docstring naming `prd.md` section 3 so the per-requirement docstring grep keeps working. `tests/test_store.py` is the natural home; keep it under 200 lines. No production code changes for this Target — the behavior is already correct (evidence-2 S-sec3); what is missing is the guard. | `tests/test_store.py` (or a new `tests/test_store_path.py`) | I15 / G2-5 | A named test asserting the `~/.notes.json` fallback passes in isolation, mentions `prd.md` section 3 in its docstring, and the real `$HOME/.notes.json` is still absent after the whole suite runs | V3-5, V3-16 |

# Preservation constraints
| id | behavior to preserve | verified in (evidence id) | how to check (V-id) |
|---|---|---|---|
| P3-1 | A usage error — no subcommand, an unknown subcommand, a missing or extra argument, an unknown or misplaced flag — exits 2, prints zero bytes on stdout, prints exactly one `usage:` line to stderr that names `add`, `list`, `done` **and** `search`, prints no traceback, and creates no store file | evidence-2 V2-3, P2-1 (and P2-1 was verified@evidence-1 V1-1/V1-2) | V3-6 |
| P3-2 | `add <text>` exits 0 and a following `list` prints one line per note in insertion order as `<n>. [ ] <text>` from `n = 1`, with `[x]` in place of `[ ]` for a done note, correct across the 9→10 boundary | evidence-2 P2-2, S-R1, S-R2 | V3-7 |
| P3-3 | `list` against an empty or absent store prints exactly `no notes` (9 bytes including the newline) and exits 0, creating neither the store file nor any parent directory — with `--color` as well as without | evidence-2 P2-3, V2-6 | V3-8 |
| P3-4 | The on-disk store is exactly `{"notes": [{"text": <str>, "done": <bool>}]}`, and no escape byte ever reaches the file, including after a `list --color` or `search` run | evidence-2 P2-4, V2-10 | V3-9 |
| P3-5 | A store file that is not valid JSON or whose shape does not match makes **every** subcommand exit 4, print zero bytes on stdout, print a stderr message naming the store path, print no traceback, and leave the file byte-identical | evidence-2 P2-5, V2-9 (45-run matrix, 0 deviations) | V3-4 |
| P3-6 | `done <n>` for a canonical in-range `n` exits 0 and is idempotent; each of the 24 non-canonical or out-of-range spellings evidence-2 exercised exits 3, prints zero bytes on stdout, names the given argument on stderr, prints no traceback and leaves the store byte-identical | evidence-2 V2-7, P2-6, S-R4 | V3-10 |
| P3-7 | `search <query>` matches a case-insensitive substring of the text, prints matches in the `list` format at their **full-list** positions (so a number from `search` can be fed to `done`), prints exactly `no matches` with exit 0 when nothing matches, never creates the store, and emits zero escape bytes even on a real pty | evidence-2 V2-1, V2-2, P2-7, S-R7 | V3-11 |
| P3-8 | `list --color` wraps only a done note's text as `<n>. [x] \x1b[2m<text>\x1b[0m`, leaving the `<n>. [x] ` prefix and every not-done line byte-identical to plain `list`; the flag alone decides, never `isatty` — plain `list` emits zero `0x1b` bytes on a pty and on a pipe, `list --color` emits them on both | evidence-2 V2-4, V2-5, S-R8 | V3-12 |
| P3-9 | Only `src/notes/` and `tests/` change; `pyproject.toml` and `requirements.txt` stay byte-identical to the project seed; every import in `src/notes/` and `tests/` is standard library or the package itself; the installed dependency set stays pytest's tree plus editable `notes` | evidence-2 V2-11, P2-8 | V3-13 |
| P3-10 | Every `.py` file under `src/notes/` and `tests/` parses with `feature_version=(3, 9)`, contains no `match` statement and no PEP 604 runtime annotation, byte-compiles, and the whole suite passes under real CPython 3.9 | evidence-2 V2-12 (`qa-2-py39run.log`, 3.9.25) | V3-14 |
| P3-11 | Every function defined in the files matched by `src/notes/*.py tests/*.py` is under 50 lines | evidence-2 V2-12 (longest 26 in tests, 18 in `src/notes`) | V3-15 |
| P3-12 | The suite is green with at least 31 passing tests, the count agrees with the gate, `tests/test_smoke.py` passes on its own, each of `R1`…`R8` is named in at least one test docstring, and no test reads or writes the real home directory — any test exercising the default path redirects `HOME` (or the home lookup) to a `tmp_path` first, and the real `$HOME/.notes.json` is absent after the suite | evidence-2 V2-8, P2-10 | V3-2, V3-16 |

Scope note for P3-11 and for T3-1's size rule: both are graded over exactly the files matched by `src/notes/*.py tests/*.py`, which is exactly what V3-1 and V3-15 measure. plan-2's P2-9 said "every module" while V2-12 measured `src/notes/*.py` only (evidence-2 G2-1's note to the Planner); the constraint text and the command now name the same set, and the module-size clause is deliberately **not** a Preservation constraint this iteration because it is currently failing — it is T3-1's `done_when`.

# Validation requirements
| id | target | kind | method (command, expected result, what to observe) |
|---|---|---|---|
| V3-1 | T3-1 | code | `cd <workspace> && wc -l src/notes/*.py tests/*.py` — every listed file, including every new `tests/test_cli_*.py` and `tests/conftest.py`, is **≤ 200** lines. Also `git diff --name-only b090366..HEAD -- tests/` to record which files were added/removed, and `git diff --stat b090366..HEAD -- src/` to confirm T3-1 itself touched no production file beyond what T3-2 needs (`src/notes/store.py` only). |
| V3-2 | T3-1, P3-12 | runtime | `.venv/bin/python -m pytest -q` → green, **≥ 31 passed**, and the count equals the `tests:` line of `gate-3.md`. Then `.venv/bin/python -m pytest -q tests/test_smoke.py` → `1 passed`. Then, for each of `R1`…`R8`, `grep -rn "R<n>" tests/` → at least one hit, so no docstring was lost in the move. Note the number of collected tests and compare with evidence-2's 31: it may only rise. |
| V3-3 | T3-2 | runtime | `d=$(mktemp -d); mkdir -p "$d/ro"; chmod 500 "$d/ro"` then, for both `NOTES_FILE="$d/ro/x.json"` (parent exists) and `NOTES_FILE="$d/ro/sub/x.json"` (parent must be created), run `.venv/bin/python -m notes add hi` and — against a valid store copied into a writable place first, then made read-only — `.venv/bin/python -m notes done 1`. Expect `rc=4` every time, `0` stdout bytes, exactly one stderr line, `grep -c "<the store path>"` = 1 on that line, `grep -c Traceback` = 0. Finish with `chmod 700 "$d/ro"`. |
| V3-4 | P3-5 (under pressure from T3-2) | runtime | Re-run the 45-case corrupt-store matrix from `hoh/notes/logs/qa-2-probe.sh` section 8 — 9 malformed payloads × {`list`, `add x`, `done 1`, `search x`, `list --color`} — and expect **0 deviations**: rc 4, empty stdout, the path named on stderr, no traceback, and the store file's sha256 unchanged in all 45 runs. This is the constraint the new `StoreError` path in `save` is most likely to disturb, since it shares exit 4. |
| V3-5 | T3-3 | runtime | `grep -rln "notes.json" tests/` locates the new test; run it alone, e.g. `.venv/bin/python -m pytest -q tests/test_store.py::<the test name>` → `1 passed`, and `grep -n "section 3" tests/<file>` shows the docstring naming `prd.md` section 3. Independently reproduce the behavior: with `HOME` pointed at a scratch directory and `NOTES_FILE` unset, `list` → `no notes` rc 0 with no file created, `add x` → rc 0 creating `$HOME/.notes.json`, `list` reads it back; repeat with `NOTES_FILE=""` and expect the same fallback. |
| V3-6 | P3-1 | runtime | The nine usage-error forms of evidence-2 `qa-2-cli.log:34-51` (bare invocation, `bogus`, `add`, `done`, `search`, `list extra`, `add a b`, `search a b`, `done 1 2`) plus the seven flag-misuse forms of `:77-84` (`list --colour`, `list -c`, `list --color extra`, `list extra --color`, `--color list`, `list --color --color`, `list --COLOR`): each `rc=2`, `0` stdout bytes, `usage line count: 1`, `grep -c` = 1 for each of `add`/`list`/`done`/`search` on that line, `grep -c Traceback` = 0, and no store file created (`test -e "$NOTES_FILE"` → non-zero). |
| V3-7 | P3-2 | runtime | With `NOTES_FILE` at a fresh temp path: three `add`s then `list` → the three lines in insertion order as `<n>. [ ] <text>` from 1; then a ten-note store with `done 1` and `done 10` → `list` shows `1.`…`10.` with `[x]` on exactly lines 1 and 10 and the 9→10 boundary intact (`9. [ ] note nine`, `10. [x] note ten` for whichever are marked). Compare the bytes with evidence-2 `qa-2-cli.log:120-129`. |
| V3-8 | P3-3 | runtime | `NOTES_FILE` at a path three directories deep that does not exist: `list` → rc 0, stdout exactly `no notes`, `wc -c` = 9, and `test -e` non-zero for the file, its parent and its grandparent. Repeat with `list --color` → same result plus `0` escape bytes. |
| V3-9 | P3-4 | runtime | `add`, `add`, `done 2`, `list --color`, `search x` against one temp store, then `.venv/bin/python -c` reading the file: top-level keys exactly `['notes']`, each note's keys exactly `{'text','done'}`, `text` a `str`, `done` a `bool`, and the count of `0x1b` bytes in the file is `0`. |
| V3-10 | P3-6 | runtime | Against a ten-note store whose sha256 is recorded first, run `done` with each of the 24 arguments listed in evidence-2 V2-7 (`" 1 "`, `"1 "`, `" 1"`, `1_0`, `1__0`, `+1`, `+10`, `010`, `00`, `0010`, `0x1`, `1e0`, `0`, `-1`, `abc`, `1.5`, `99`, `11`, `""`, `" "`, `\t1`, `١`, `٢`, `１`): every one `rc=3`, `0` stdout bytes, the given argument named on stderr, no traceback, sha256 unchanged. Then `done 1` and `done 10` → rc 0, and `done 1` again → rc 0 with the store unchanged (idempotent). |
| V3-11 | P3-7 | runtime | Store `Buy Milk`, `call mom`, `Milk run`: `search milk`, `search MILK`, `search Milk` each rc 0 with stdout exactly `1. [ ] Buy Milk` + `3. [ ] Milk run` (note the positions 1 and 3, not 1 and 2); `search "uy mi"` → `1. [ ] Buy Milk`; `search zzz` → rc 0, stdout exactly `no matches`, `wc -c` = 11; `search zzz` against an absent 3-deep path leaves file, parent and grandparent absent; `search milk \| cat -v` shows no `^[`, and `search` run on a real pty (reuse `hoh/notes/logs/qa-2-pty.py`) reports `ESC count = 0`. |
| V3-12 | P3-8 | runtime | Store one not-done and one done note. `list --color \| cat -v` → `1. [ ] plain one` then `2. [x] ^[[2mdone one^[[0m`; capture raw bytes and expect exactly `b'1. [ ] plain one\n2. [x] \x1b[2mdone one\x1b[0m\n'` against plain `list`'s `b'1. [ ] plain one\n2. [x] done one\n'`; plain escape count 0, colored escape count 2, not-done line identical, prefix unchanged. Then the pty harness (`qa-2-pty.py`): the sanity child reports `isatty=True`, plain `list` on that pty gives `ESC count = 0`, `list --color` on the pty and on a pipe both give `ESC count = 2` — the flag, not `isatty`, decides. |
| V3-13 | P3-9 | code | `git diff --name-only b090366..HEAD` → only paths under `src/notes/` and `tests/`; the same diff restricted `-- pyproject.toml requirements.txt` → empty; `git diff aa71252..HEAD -- pyproject.toml` → empty; `cat requirements.txt` → `pytest` only; `grep -rn "^import \|^from " src/notes tests` → standard library or `notes` itself only; `.venv/bin/pip list` → pytest's tree plus editable `notes` and nothing else. |
| V3-14 | P3-10 | code + runtime | `.venv/bin/python -c` with `ast.parse(..., feature_version=(3, 9))` over every `.py` file in `src/notes/` and `tests/` → 0 failures; `grep -rn "match .*:\|: *[A-Za-z]* *| *None" src/notes` shows no `match` statement and no PEP 604 runtime annotation; `.venv/bin/python -m compileall -q src/notes tests` → rc 0. Then the whole suite under the scratch CPython 3.9.25 that iterations 1–2 used (outside the repository): `python3.9 -m pytest -q` → green with the same passed count, and spot-check the new exit-4 unwritable case and the default-path behavior under it. Confirm `git status --porcelain` is empty afterwards. |
| V3-15 | P3-11 | code | Walk the AST of exactly the files matched by `src/notes/*.py tests/*.py` and report each function's `end_lineno - lineno + 1`; the longest must be **< 50**. Report the longest function per file so the number is auditable, as evidence-2 V2-12 did. |
| V3-16 | P3-12, T3-3 | runtime | `test -e "$HOME/.notes.json"; echo $?` → non-zero **before and after** `.venv/bin/python -m pytest -q`, i.e. the suite never creates the real store. Then `grep -rn "Path.home\|expanduser\|\.notes\.json" tests/` — hits are allowed now (T3-3 needs them), but every hit must sit in a test that first redirects the home directory to `tmp_path` (`monkeypatch.setenv("HOME"` or a `monkeypatch.setattr` on the home lookup). Report any hit that does not. |
| V3-17 | P3-8 (appearance half) | visual | Not automatable, by prd.md:70-73's own design. A human runs, in their usual terminal: `export NOTES_FILE=$(mktemp -u); .venv/bin/python -m notes add "still to do" && .venv/bin/python -m notes add "already done" && .venv/bin/python -m notes done 2 && .venv/bin/python -m notes list --color` and says whether the dimmed line is legible on their background. QA verifies the bytes (V3-12) and files the appearance as a gap; linked to owner=`user` issue **I9**. Never a Target. |
| V3-18 | P3-11, T3-1 | code | Plan self-consistency check, the defect evidence-2 G2-1 raised against plan-2: read the Preservation and Validation tables above and confirm that every size/length constraint names the same subject its command measures — P3-11 says "the files matched by `src/notes/*.py tests/*.py`" and V3-15 measures exactly that glob; T3-1's `done_when` and V3-1 use the same glob. Report any constraint graded against a wider subject than its own command measures. Linked to owner=`orchestrator` issue **I17**. |

# Deferred
This is the final iteration, so everything below is what a human is left holding.

- **I13 / G2-3 — `add ""` and `add "   "` store a blank note (owner developer, still open).** The PRD does not define empty text, so this is an extension, ranked below T3-3's unmet section-3 coverage requirement, and changing an input path that currently exits 0 on the last iteration risks R1's verified behavior for no PRD gain. The decision is already recorded and unchanged: treat empty or whitespace-only text as a usage error (exit 2 with the usage line), no new exit code, with a test. One small commit for whoever picks the repository up next. It should be decided together with I16, since both are questions about what the CLI does with input the PRD never described.
- **I9 / G2-4 — R8's dim rendering (owner user).** The bytes are verified (`\x1b[2m` opened before the text, `\x1b[0m` after, nothing around the prefix, nothing on not-done lines). Whether dim is legible on the user's background is the manual gap prd.md:70-73 requires QA to leave open. Because of it `qa_status: pass` is unreachable by construction — that is not a statement about the Developer's work, and the PRD's Definition of Done explicitly allows it. Run V3-17 to close it.
- **I16 / G2-6 — R8 wording (owner user).** Does "without `--color`, no escape sequence appears in the output at all" cover escape bytes the user themselves stored in a note's text? Today such bytes are echoed verbatim, and have been since iteration 1 (`git show 0cdf922:src/notes/cli.py:37-39`), so this is not a regression. Resolving it toward sanitization would change the exact output strings R2 fixes, so it needs a PRD amendment before any code. I did not open a Target for it.
- **I17 — the plan-2 P2-9/V2-12 mismatch (owner orchestrator).** Fixed in this document rather than in the repository: P3-11, T3-1, V3-1 and V3-15 all name `src/notes/*.py tests/*.py`, and V3-18 asks QA to confirm the plan is self-consistent. The general lesson belongs to the Planner role contract, which the Developer cannot edit — hence owner `orchestrator`.
- **Two behaviors the PRD leaves undefined, decided here rather than filed as issues.** (a) `search ""` matches every note: the empty string is a substring of every string, so this follows from R7's own substring rule and needs no change. (b) `search --color` and `add --color` treat `--color` as an ordinary argument: R8 scopes the flag to `list`, and R5 fixes arity elsewhere, so treating it as text is the only reading consistent with both. Neither is pinned by a test; if the user wants them frozen, the cheap follow-up is one test each, not a code change.
- **Not attempted at all**: a `delete` subcommand (nothing in the PRD asks for one, though its absence is what makes a stray blank note permanent — see I13), and any change to `pyproject.toml`, `requirements.txt` or `hoh/`, all of which prd.md section 2 puts out of scope.
